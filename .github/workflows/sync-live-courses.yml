name: Sync live courses from Harvard API

on:
  schedule:
    - cron: '0 7 * * *'   # 07:00 UTC daily (3am ET / before Harvard business day)
  workflow_dispatch:        # allow manual trigger from GitHub UI

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Run sync
        env:
          HARVARD_API_KEY: ${{ secrets.HARVARD_API_KEY }}
          SUPABASE_URL:    ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY:    ${{ secrets.SUPABASE_KEY }}
          # Destructive stale-row deletion remains disabled unless production
          # coverage and the database synced_at trigger are independently verified.
          SYNC_ALLOW_STALE_DELETE: 'false'
        run: python scripts/sync_live_courses.py
